node_modules
*.local

# The build output is not committed. The Pages workflow
# (.github/workflows/static.yml) runs `npm run build` and uploads dist/ from
# the runner, so a local build is yours alone and never needs pushing.
dist
